# Copy to `.env` and fill in (never commit `.env`).
# Create an app at https://developer.spotify.com/dashboard
# If credentials were ever exposed, rotate the client secret in the dashboard.

SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
